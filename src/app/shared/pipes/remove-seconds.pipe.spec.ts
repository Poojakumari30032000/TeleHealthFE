import { RemoveSecondsPipe } from './remove-seconds.pipe';

describe('RemoveSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
